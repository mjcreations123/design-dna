interface:
  display_name: "Design DNA"
  short_description: "Specific, contemporary web design craft"
  default_prompt: "Use $design-dna to ground this web task in project evidence, implement real states, and perform rendered specificity review."
policy:
  allow_implicit_invocation: true
