# Local debug note — untracked

Keep this file exactly as supplied.

The preferences route briefly loses focus when the browser restores a prior form value. This note belongs to separate investigation `PREF-17`; do not resolve or reformat it during the requested form change.
