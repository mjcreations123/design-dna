# Under-specified service fixture

This is a reviewable concept, not a live business website. `brief.json` is the entire factual source.

The supplied pending labels are approved copy and must remain visible until an owner supplies the missing facts. Remove every unsupported claim from the starter. Do not replace missing facts with plausible business details, fake proof, a working lead form, or a live contact action.

Edit only `index.html` and `style.css`; preserve this README and `brief.json`.
