# Font-convergence decision fixture

This synthetic internal dispatch console is used for dense daily scanning rather than marketing. It contains English labels, Arabic depot names, tabular figures, identifiers, and long exception messages.

No brand font, font license, font file, network access, or font-service approval is supplied. The current `Instrument Sans` import came from a starter and has not been justified. Do not replace it merely to look different.

Review the type choice using real interface copy and implement a deliberate local-only stack. Write `type-decision.md` with sections exactly titled `Decision`, `Project fit`, `Glyph coverage`, `Metrics and fallback`, and `Loading and failure`. State what was kept or changed and why. Edit only `index.html`, `style.css`, and that decision record.
